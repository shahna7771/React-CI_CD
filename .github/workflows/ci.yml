name: "sample React-CI pipeline"

on:
    push:
        branches:
            - master
            - main


jobs:
    build-and-test:
        runs-on: ubuntu-latest
        steps:
            - name: "setup node Js"
              uses: actions/setup-node@v4
              with:
                node-version: '21'


